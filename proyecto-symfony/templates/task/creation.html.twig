{# 480. Método crear tareas #}
{% extends 'base.html.twig' %}

{% block title %}
    
    {# 484. Edicion de tareas #}
    {# is defined comprueba que existe la variable edit, es un isset() #}
    {% if edit is defined %}
        Editar tarea
    {% else %}
        Crear tarea
    {% endif %}

{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h2>
            {# 484. Edicion de tareas #}
            {% if edit is defined %}
                Editar tarea
            {% else %}
                Crear tarea
            {% endif %}
        </h2>
        
        {# 481. Crear tareas #}
        
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        
    </div>
{% endblock %}
